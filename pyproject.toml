[project]
name = "tiny-gpt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kratika246", email = "guptakratika246.com" }
]
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.11.2",
    "notebook>=7.6.2",
    "numpy>=2.5.3",
    "torch>=2.14.0",
    "tornado<6.5",
]

[project.scripts]
tiny-gpt = "tiny_gpt:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
